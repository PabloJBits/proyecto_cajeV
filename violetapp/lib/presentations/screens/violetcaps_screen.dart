import 'package:flutter/material.dart';

class VioletCapsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Centros de Apoyo')),
      body: Center(
        child: Text('Aquí encontrarás recursos educativos sobre derechos y protección.'),
      ),
    );
  }
}
