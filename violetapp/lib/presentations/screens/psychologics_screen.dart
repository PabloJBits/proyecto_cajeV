import 'package:flutter/material.dart';

class PsychologicsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Psicólogos & Psicólogas')),
      body: Center(
        child: Text('Aquí encontrarás un listado de psicólogos.'),
      ),
    );
  }
}
