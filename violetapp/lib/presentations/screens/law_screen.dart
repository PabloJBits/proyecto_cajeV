import 'package:flutter/material.dart';

class LawScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Apoyo Legal')),
      body: Center(
        child: Text('Aquí encontrarás recursos de apoyo legal.'),
      ),
    );
  }
}
