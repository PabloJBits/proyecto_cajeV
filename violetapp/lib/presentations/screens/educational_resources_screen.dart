import 'package:flutter/material.dart';

class EducationalResourcesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Recursos Educativos')),
      body: Center(
        child: Text('Aquí encontrarás recursos educativos sobre derechos y protección.'),
      ),
    );
  }
}
